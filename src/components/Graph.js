import React from 'react';
function Graph(data) {
    console.log("rendering and data is -" + JSON.stringify(data?.[0]))
    return ( 
        <div>
            
        </div>
    )
}

export default Graph;